# Server port (default: 3000)
PORT=3000

# Optional: Facebook App Token for server-side cache purging
# Used to purge Facebook's sharing cache via the Graph API
# If not provided, Facebook cache purging will be skipped on the server side
# Get one from: https://developers.facebook.com/tools/explorer/
FACEBOOK_APP_TOKEN=
