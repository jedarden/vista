# app.js Structure Analysis

**File:** `src/public/app.js` (9,998 lines)  
**Analysis Date:** 2025-01-24  
**Bead:** bf-152i0

## Overview

app.js is the main frontend application file for VISTA, a social media preview testing tool. It implements a comprehensive interface for analyzing how web pages appear across 31+ different platforms and contexts.

## Major Code Sections

### 1. **State Management** (lines 1-15)
- `currentData` - Current inspection results
- `currentMode` - Application mode ('url'|'paste'|'compare'|'sitemap')
- `cardContextState` - Per-platform context/theme state
- `compareData` - Comparison mode state
- `hasCelebratedPerfectScore` - One-time celebration tracker
- `isFreshFetch` - Distinguishes fresh fetches from cached/restored results
- `currentTab` - Active tab state
- `pendingWhatIfTags` - Stores What If tags from hash before data loads

### 2. **Platform Configuration** (lines 14-31)
- `fetchPlatformConfig()` - Fetches platform→skeleton-type mapping from `/api/platforms`
- Ensures client uses same platform configuration as server
- Fallback to minimal mapping if fetch fails

### 3. **Debug Flags** (lines 33-51)
- `DEBUG_SMART_ORDERING` - Enables detailed logging for smart ordering functionality
- Can be toggled from browser console
- Logs platform reordering, scores, and localStorage operations

### 4. **Theme State** (lines 58-115)
- `initTheme()` - Initialize from localStorage or system preference
- `applyTheme()` - Apply theme to document and save to localStorage
- `toggleGlobalTheme()` - Toggle between dark/light modes
- Re-renders cards to update theme-aware previews

### 5. **DOM References** (lines 117-222)
- Comprehensive selector references for all UI elements
- Organized by feature: hero, forms, results, modals, tools
- Badge modal, QR modal, OG Generator, Sitemap DOM refs

### 6. **Event Listeners** (lines 229-370)
- Form submissions (URL, paste, compare, sitemap)
- Paste detection with smart content type detection
- Suggestion chip actions and dismissal
- Navigation mode switching
- Modal interactions (badge, QR)
- OG Generator event listeners
- Tab switching with keyboard navigation (ARIA tablist pattern)
- Example chip interactions

### 7. **URL Hash State Management** (lines 381-507)
- `getHashState()` - Parse hash into state object
- `updateHash()` - Update hash based on current state
- `restoreHashState()` - Restore tab, compare mode, What If tags from hash
- Supports shareable URLs with state persistence

### 8. **Mode Switching** (lines 512-564)
- `switchMode()` - Switch between URL/Paste/Compare/Sitemap modes
- Updates UI visibility and navigation state
- Clears compare mode parameters when leaving compare mode

### 9. **Paste Detection** (lines 566-629)
- `handlePasteDetection()` - Detects HTML, multiple URLs, sitemaps
- `showSuggestionChip()` - Shows UI suggestions for detected content
- `clearSuggestionChips()` - Remove suggestion UI elements

### 10. **Inspection Functions** (lines 631-1022)

#### Progressive Loading Implementation
- `progressiveLoad()` - Main progressive loading orchestrator
  1. Show skeleton grid instantly
  2. Call `/api/preview/meta`, populate score and text cards
  3. Call `/api/preview/images` and `/api/preview/headers` in parallel
  4. Fill in image cards and diagnostics as they arrive
  5. Hide loading state when all complete

- `fetchImagesAndHeaders()` - Fetch image data
- `fetchHeaders()` - Fetch header analysis
- `mergeData()` - Merge metadata, images, and headers
- `finalizeProgressiveLoad()` - Complete progressive load with client-side verification

#### Client-Side Verification
- `verifyClientSideTags()` - Parse HTML in real DOM (executes JavaScript) and compare with server-parsed tags
- Returns diagnostic findings for client-side-only tags
- Uses iframe to render HTML and extract meta tags after JS execution

#### Core Inspection Functions
- `inspectUrl()` - Inspect a URL
- `inspectHtml()` - Inspect pasted HTML
- `handleResult()` - Process and render inspection results

### 11. **Perfect Score Celebration** (lines 1111-1213)
- `isPerfectScore()` - Check if all 31 platforms have A+ grade
- `triggerConfetti()` - Show confetti animation
- `checkAndCelebrate()` - One-time celebration per session
- `showPerfectScoreToast()` - Show toast with share button

### 12. **Summary Bar Rendering** (lines 1215-1238)
- `renderSummaryBar()` - Render overall grade, counts, URL
- Updates diagnostic badge count
- Triggers perfect score celebration

### 13. **Platform Configuration Constants** (lines 1240-1499)

#### Platform Groups
- Social & Microblogging (10 platforms)
- Messaging (11 platforms)
- Collaboration & Productivity (7 platforms)
- Content, Email & RSS (5 platforms)

#### Platform Data Structures
- `PLATFORM_ICONS` - Emoji icons for each platform
- `PLATFORM_NAMES` - Human-readable platform names
- `PLATFORM_CROPS` - Aspect ratios, crop modes, display sizes
- `CATEGORY_COLORS` - Color coding by platform category
- `PLATFORM_CHAR_LIMITS` - Title/description character limits
- `SAFE_ZONE_COLOR` - Distinct color for crop intersection overlay

### 14. **Skeleton Rendering** (lines 1500-1581)
- `getSkeletonType()` - Get skeleton type for platform
- `getSkeletonHtml()` - Generate skeleton HTML based on type (tall/short/text-only)
- `setupGroupHeader()` - Make group headers accessible disclosure toggles
- `renderSkeletons()` - Render skeleton cards for all platforms
- `showSkeletonCards()` - Wrapper function for clarity

### 15. **Preview Rendering** (lines 1583-1999+)
- `renderPreviews()` - Main preview rendering function
  - Prevents concurrent renders with guard flags
  - Queues renders during smart ordering
  - Uses custom card order if available
  - Implements "Filter Orphan Bug" fix for stale cardOrder
  
- `renderTextPreviewsOnly()` - Progressive loading: show text immediately
- `buildTextOnlyCard()` - Build card with text content and loading indicators
- `updatePreviewsWithImages()` - Update cards with image data as it arrives
- `buildCard()` - Build complete platform preview card
- `renderPlatformCard()` - Render card body based on platform type

### 16. **What If Toggle** (lines 8117-8266)
- **`toggleWhatIfMode()`** - Enable/disable What If mode
- **`showWhatIfPanel()`** - Show panel with tag toggles
- **`applyWhatIfChanges()`** - Re-render previews with tags disabled

**What If Mode Features:**
- Toggle off specific meta tags to see fallback behavior
- Supports Open Graph, Twitter Card, and basic tags
- Persists state to URL hash
- Queues operations during smart ordering to prevent race conditions
- Uses guard flag `isFilterOperation` to prevent smart order resets

**Filter-Related Code:**
- `disabledTags` Set tracks which tags are disabled
- Hash state `without` parameter persists disabled tags
- `modifiedMeta` creates copy of metadata with disabled tags removed
- Re-runs scoring and preview rendering with modified metadata

### 17. **Platform Preferences & Smart Ordering** (lines 6263-6281)
- **`platformPrefs`** object structure:
  - `favorites` - Set of favorite platforms
  - `hidden` - Set of hidden platforms
  - `columnCount` - Grid column count
  - `smartOrdering` - Enable/disable smart ordering
  - `cardOrder` - Custom platform order per group
  - `cardOrderMetadata` - Order modification tracking

**Guard Flags for Race Prevention:**
- `isApplyingSmartOrder` - Smart ordering in progress
- `pendingApplySmartOrder` - Queued smart order operation
- `isRendering` - Prevent concurrent renders
- `isFilterOperation` - Prevent smart order resets during filter changes
- `isSmartOrderingActive` - Track active smart ordering state
- `pendingFilterOperations` - Queue filter operations during smart ordering

### 18. **Tab Switching** (lines 4572-4587)
- **`switchTab()`** - Switch between tab panes
- Supports: previews, editor, diagnostics, codesnippet, templates, cachehub, customization
- ARIA tab pattern with aria-selected and roving tabindex
- Updates hash to persist active tab

### 19. **Recent Inspections** (lines 4589-4622)
- `saveToRecents()` - Save inspection to localStorage
- `loadRecentsList()` - Load recent inspections
- `loadRecents()` - Initialize recent inspections on page load
- `renderRecentBar()` - Render recent inspection chips

### 20. **Share & QR Code** (lines 4624+)
- `shareResults()` - Copy shareable URL to clipboard
- `generateQRCode()` - Generate QR code for URL
- QR modal interactions

### 21. **Command Palette** (lines 6283-6286)
- Command palette state management
- Recent commands tracking

### 22. **Editor** (lines 6289-6356+)
- `initEditor()` - Initialize editor with inspection data
- `populateEditorForm()` - Populate form fields
- `updateEditorFieldImpactLabels()` - Show platform impact per field
- Editor state management (original, edited, dirty)

### 23. **Additional Features** (throughout file)
- Sitemap analysis
- Compare mode
- OG Generator
- Badge generation
- Cache Hub
- Cropper tool
- Sccreenshot functionality
- Context card toggling
- Theme switching per platform

## Filter-Related Code Locations

### Primary Filter: What If Mode (lines 8117-8266)
- **State:** `whatIfMode`, `disabledTags`
- **UI:** What If panel with tag toggles
- **Function:** `toggleWhatIfMode()`, `showWhatIfPanel()`, `applyWhatIfChanges()`
- **Persistence:** URL hash `without` parameter
- **Impact:** Modifies metadata, re-runs scoring and preview rendering

### Secondary Filter: Platform Visibility (lines 6263-6280)
- **State:** `platformPrefs.hidden`
- **Function:** Hide/show specific platforms
- **Impact:** Filter which platforms appear in preview grid

### Tertiary Filter: Smart Ordering (throughout rendering code)
- **State:** `platformPrefs.smartOrdering`, `cardOrder`
- **Function:** Reorder platforms based on page type detection
- **Impact:** Changes platform display order

## Code Organization Patterns

### Progressive Enhancement
- Skeleton loading → Text-only previews → Full image previews
- Client-side verification after server-side parsing
- Gradual feature initialization

### Race Condition Prevention
- Multiple guard flags (`isApplyingSmartOrder`, `isRendering`, `isFilterOperation`)
- Operation queuing during critical sections
- State restoration after operations complete

### Accessibility
- ARIA tab patterns (role="tablist", aria-selected, roving tabindex)
- Accessible disclosure toggles (role="button", aria-expanded)
- Screen reader announcements
- Keyboard navigation support

### State Persistence
- URL hash for shareable state
- localStorage for preferences and recent items
- Platform preferences with custom ordering

## Key Entry Points

1. **Page Load:** `window.addEventListener('DOMContentLoaded')` (line 491)
2. **URL Inspection:** `inspectUrl()` (line 911)
3. **HTML Paste:** `inspectHtml()` (line 929)
4. **Tab Switch:** `switchTab()` (line 4572)
5. **What If Toggle:** `toggleWhatIfMode()` (line 8121)

## Dependencies

The file references several external tools and libraries:
- `canvas-confetti` - Celebration animations
- `QRCode` - QR code generation
- `frames-theme.js` - Theme management (loaded before app.js)
- Various client-side utilities for DOM manipulation

## Summary

app.js is a well-organized, feature-rich frontend application that implements:

1. **Multi-platform social media preview testing** across 31+ platforms
2. **Progressive loading** for fast perceived performance
3. **Advanced filtering** via What If mode (tag toggling)
4. **Smart ordering** with race condition prevention
5. **Comprehensive state management** with hash and localStorage persistence
6. **Accessibility-first design** with ARIA patterns and keyboard navigation
7. **Real-time editing** with instant preview updates

The filter-related code is primarily concentrated in:
- **What If Mode** (lines 8117-8266) - Main filtering functionality
- **Platform Preferences** (lines 6263-6280) - Platform visibility and ordering
- **Rendering functions** - Guard flags and operation queuing to prevent filter changes from triggering unwanted smart order resets
