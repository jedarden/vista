# Vista app.js addHook Search Results

**Task:** Search through the Vista app.js file to find all addHook function calls, regardless of event type.

**Date:** 2026-07-24
**File:** `/home/coding/vista/src/public/app.js`

---

## Summary

**No addHook patterns found in Vista app.js.** Vista does not implement a traditional hook registration system where you would call `addHook('event-name', callback)`.

---

## Search Results

### addHook Patterns Found: **0**

Comprehensive search for the following patterns returned **no results**:
- `addHook` (exact match)
- `.addHook(` (method call)
- `addHook:` (object property)
- `addHook '` (string parameter)
- `addHook "` (string parameter)
- Case-insensitive variations

### Search Scope
- **File analyzed:** `/home/coding/vista/src/public/app.js` (9,998 lines, 368KB)
- **Searched patterns:** All variations of `addHook` function calls
- **Result:** No matches found

---

## What Vista Uses Instead

Vista uses **direct event listeners** and **callback patterns** rather than a hook registration system:

### 1. addEventListener Patterns (Lines 230-296)

```javascript
// Form submission handlers
urlForm.addEventListener('submit', (e) => { e.preventDefault(); inspectUrl(urlInput.value.trim()); });
pasteForm.addEventListener('submit', (e) => { e.preventDefault(); inspectHtml(htmlInput.value.trim(), baseUrlInput.value.trim()); });

// Input handlers
urlInput.addEventListener('paste', async (e) => { ... });

// Click handlers
document.addEventListener('click', (e) => { ... });
$('#switchToPaste').addEventListener('click', () => switchMode('paste'));
$('#switchToUrl').addEventListener('click', () => switchMode('url'));

// Modal handlers
badgeBtn?.addEventListener('click', openBadgeModal);
badgeModalClose?.addEventListener('click', closeBadgeModal);
```

### 2. Hook Comments (Lines 3559, 8950-8976)

```javascript
// single hook keeps the legend in sync with the overlays on screen.

// ── Hook into renderDiagnostics for tracking ──
// ── Hook into handleResult for smart ordering ──
// Store reference for use in hook
console.log('[handleResult hook] smartOrdering enabled:', platformPrefs.smartOrdering);
```

**Note:** These are comments describing hooking into existing functions, not `addHook` function calls.

---

## Codebase-Wide Search Results

Searched entire Vista codebase for `addHook`:
- **In src/:** 0 matches
- **In node_modules/:** 2 matches (playwright dependencies only)
  - `/home/coding/vista/node_modules/playwright/lib/transform/transform.js`
  - `/home/coding/vista/node_modules/playwright/lib/third_party/pirates.js`

---

## Event Types in Vista

Since no `addHook` patterns exist, there are **no event types** to document. Vista handles events through:

- **Form events:** `submit`, `change`
- **Mouse events:** `click`, `paste`
- **Custom logic:** Direct function calls, callback patterns
- **Guard flags:** `isFilterOperation` booleans
- **Queue patterns:** `pendingFilterOperations[]` arrays

---

## Architectural Context

Vista uses a **direct callback pattern** rather than a hook registration system:

| Traditional Hook System | Vista's Approach |
|------------------------|------------------|
| `addHook('event', callback)` | `element.addEventListener('event', callback)` |
| Hook registration | Direct function assignment |
| Central hook dispatcher | Event bubbling and direct calls |
| Hook priority chains | Guard flags and queue/defer patterns |

---

## Related Documentation

This search confirms findings from previous beads:
- **bf-2dmjx:** "No addHook patterns found in Vista app.js"
- **bf-58lvk:** "document addHook filter-change search results - no patterns found"
- **bf-3w889:** "document addHook filter-change search results - no patterns found"

---

## Conclusion

**Vista does not use `addHook` function calls.** The application uses standard DOM event listeners (`addEventListener`), direct callbacks, guard flags, and queue patterns instead of a hook registration API.

This search establishes the foundation for understanding that Vista's event handling architecture is fundamentally different from hook-based systems. Any search for "filter-change hook patterns" should focus on `addEventListener` calls and related guard/queue patterns rather than `addHook` function calls.