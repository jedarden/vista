# Search for addHook Calls with Filter-Change Events

**Task:** Search app.js for all addHook calls related to filter-change events.

**Date:** 2026-07-24
**File:** `/home/coding/vista/src/public/app.js`

---

## Summary

**No `addHook('filter-change', ...)` patterns found.**

The search revealed that Vista does **not** use an `addHook` function for filter-change events. Previous documentation referencing "filter-change hook patterns" refers to architectural patterns (guard flags, queues, wrapper functions), not actual addHook API calls.

---

## Search Methodology

Searched for:
1. `addHook.*filter-change` - No results
2. `addHook` function calls - No results  
3. `filter-change` string literals - No results
4. Hook function definitions - No results

---

## Actual Filter-Change Architecture

Vista uses a **vanilla JavaScript approach** with these patterns (documented in parent bead bf-52b8f):

### 1. Guard Flag Pattern
- `isFilterOperation` boolean flag (line 6279)
- Prevents smart order resets during filter changes

### 2. Queue/Defer Pattern
- `pendingFilterOperations[]` array (line 6281)
- `queueFilterOperation()` function (lines 7942-7947)
- Defers filter operations during smart ordering

### 3. Function Wrapping
- Hook into `renderDiagnostics` (lines 8950-8955)
- Hook into `handleResult` (lines 8957-8982)

### 4. Guard Wrapper Functions
- `guardWrapper()` (external: filter-guard-wrapper.js)
- `guardWrapperWithRender()` (external: filter-guard-wrapper.js)

### 5. Centralized Guard Functions
- `isSmartOrdering()` (external: guard-utils.js)
- `isFilterOperationInProgress()` (external: guard-utils.js)

---

## Conclusion

**0 addHook calls found.** Vista implements filter-change handling through direct function calls, guard flags, and queue management rather than a formal hook/subscription system. The app uses vanilla JavaScript patterns without a custom event registration API.

---

## Related Documentation

- Parent bead: bf-52b8f - "document all filter-change hook patterns in app.js"
- Related beads: bf-1snrb, bf-2rx21, bf-8zn0u - all documented filter-related patterns

These beads documented architectural patterns (guards, queues, wrappers), not addHook API calls.
